<div class="form">
  <h3>編集する</h3>
    <div class="form_space">
        <%= form_with(model: @tweet, local: true) do |form| %>
        <%= form.text_area :text, placeholder: "今何してる？" , rows: "10",class: "text" %>
    </div>
    <div class="form_btn">
       <%= form.submit "SEND",class: "send_btn" %>
       <% end %>
    </div>
</div>